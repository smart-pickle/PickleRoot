version: '3.8'

services:
  aura-home:
    build: .
    container_name: aura-home
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    # Use server-side persistence via JSON file in /app/data
    environment:
      - NODE_ENV=production
      - PORT=80
